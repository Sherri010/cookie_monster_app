<p id="notice"><%= notice %></p>

<p> You can see this image because you set the cookie `can_has` equal to `yarly` in your browser. Otherwise the server would have redirected you back to the homepage.</p>

<%= image_tag @dessert.img_url %>

</br>


  <% if session[:favorite_dessert_id] == @dessert.id %>
    <p>Your Favorite </p>
<!-- CHANGE THIS link to reflect whether the dessert has already been favorited -->
<% else %>
<%= button_to "Save as Favorite", save_as_favorite_path(@dessert.id) %>
<%end%>